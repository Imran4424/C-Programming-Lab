#include <stdio.h>
#include <math.h>

int main()
{
	int a,b,c,d,n;
	long double sum=1.00;
	scanf("%d%d%d%d",&a,&b,&c,&n);
	for(d=1;d<=n;d++){
		sum=sum+a*(pow(b,d)+1/pow(c,d);
	}	
	printf("%Lf\n",sum);
	return 0;
}	
