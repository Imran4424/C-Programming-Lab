#include <stdio.h>
#include <string.h>

int main()
{
	char a,str[30];
	int i,sum=0;
	gets(str);
	
	puts(str);
	scanf("%c",&a);
	for(i=0;i<=30;i++){
		if(a==str[i]){
			sum=sum+1;
		}
	}		
	printf("%c is in input string: %d\n",a,sum);
	
	return 0;

}
